# 1.0.2 发布封包核验

最终 ZIP 共 5,535 个声明内容文件和一个根清单，全部逐文件 SHA256 校验通过。打包只更新发布元数据、文档和界面版本文字；已验收的 EXE/DLL/DXIL 不变。文件清单只包含分发载荷，不包含测试会话、个人 OBS 配置、开发历史或未发布实验。

同一候选运行库的 CPU DIS 原生引擎另由 ComfyUI 真实入口补验 SR/FG/组合：8 帧输入分别输出 8/15/15 帧，音轨、全片解码与展示时间戳通过。此项是共享运行库旁证，不冒充离线窗口的重复人工测试。

便携程序完整下载包及 SHA256 见 `DISTRIBUTION.json`。本版的画面、性能和硬件覆盖边界仍以 `VALIDATION.md` 为准。
