# 离线工具箱验收摘要

## 1.0.1 兼容补丁

2026-09-08，Windows 11、Intel Arc B580。使用准备发布的便携程序、内置 Python 和运行库，在中文搬迁目录运行；48 帧、24fps、带音轨的严格 CFR 实拍测试素材。

| 本轮检查 | 结果 |
| --- | --- |
| 五路线 × 超分/插帧/一条龙，默认入口 | 15/15 |
| GPU Block / GPU DIS / AMD 光流 × 三模式，兼容入口 | 9/9 |
| 三 GPU 路线一条龙、兼容入口、H.264 CPU / FFV1 | 6/6 |
| 三 GPU 路线一条龙，三项特效全开，兼容入口开/关 | 6/6 |
| 兼容开/关，解码后 RGB24 全片 SHA256 | 12/12 相同，包括特效组合 |
| 编码器启动失败 / 活跃软件编码过程中取消 | 2/2，有界退出，无未完成成片发布 |
| Python 单测 / 前端路线与模式检查 | 144/144、15/15 |
| 最终 ZIP 重新解压，中文+空格目录和源文件 | 5532/5532 内容文件哈希一致；额外三 GPU 一条龙 3/3 |

36 组新成片均检查编码格式、完整可解码性、帧数及音轨。超分为 48 帧，XeFG 为 95 帧，Intel 视频接口为 96 帧；保留各引擎真实计数语义。三 GPU 路线记录实际选中的共享入口，不能只凭请求参数认定兼容生效。

搬迁过程中发现中文安装路径导致 OpenVINO 原生窄字符模型路径打不开，已改为从运行库目录解析同一模型的相对路径，三路真机验证通过。早期测试片平均帧率异常，被 Intel 路线正确拒绝；重新生成严格 CFR 测试片后重跑，未放宽生产预检。

本轮复用 ComfyUI R4.2 已验收的兼容 EXE/着色器；没有改动光流、深度模型或 Mask 算法。B580 的同片哈希一致不代表所有显卡必然逐字节相同。A770 是兼容修复的目标设备，本完整便携包未在 A770 真机复跑；不宣称 AMD/NVIDIA 全链支持。

最终包为 820,504,642 字节，含 5,533 项文件（含清单），SHA256：`cddaece0210bb2445bbdd0283517ebb7d724307e001ed86e100e832e590ea284`。旧安装文件逐一复核未变。最终搬迁共 39 次有效成片测试，失败的测试夹具/路径调用不计入通过数字。

以下长跑数据只属于 1.0.0，不计入 1.0.1 的新验收成绩。

## 1.0.0 历史验收

日期：2026-09-08。平台：Windows 11、Intel Arc B580。结果仅适用于此轮便携构建，不借用其他分支的稳定性结论。

| 检查 | 结果 |
| --- | --- |
| 五路线 × 超分/插帧/一条龙，8 帧输入 | 15/15，通过帧数、音轨、编码、解码与时间戳验证 |
| 三 GPU 路线一条龙、三项特效 | 3/3 |
| 额外 HEVC GPU / H.264 CPU / HEVC CPU / FFV1 | 4/4 |
| GPU Block，1200 输入帧超分＋插帧 | 2399 帧，48fps，音轨保留，36.07 秒 |
| CPU DIS，同一 1200 帧素材 | 2399 帧，48fps，音轨保留，150.91 秒 |
| 竖屏赛车，300 帧，GPU Block 一条龙、HEVC、三项特效 | 599 帧，音轨和时间戳通过，20.93 秒 |
| 实际窗口取消任务、CPU DIS 处理中确认关闭 | 均取消；没有发布未完成成片；应用及其子进程退出 |
| 编码器提前失败、软件编码处理中取消 | 2/2，有界退出，无未完成成片发布 |
| 中文＋空格目录重新解压 | 启动、私有运行环境与界面验证通过 |

1200 帧是原片循环后重新固定时间戳的 50 秒测试素材；最初构造错误的循环素材被输入预检拒绝，未用于成功结论。关窗自动化第一次因测试端确认弹窗失败而超时，之后在实际 CPU DIS 运行时重试并通过。耗时是单次交付测试，不是三轮交错性能基准。

取消的任务可在自身私有诊断目录保留 `.partial` 供排错；不会将其当作用户成片发布。没有宣称目录绝对无残留。

轻量回归、发布保护测试、Python/JavaScript 语法与文件完整性检查均通过。最终文件 SHA256 随 Release 提供。运行时二进制与此次真实处理测试相同；最后打包只更新发布说明、界面版本文字和清单。

尚未覆盖：A770/AMD/NVIDIA 真机、Windows 10、所有 DPI/显示配置、电影时长和多小时稳定性。GPU DIS、AMD 光流继续标为实验项；不存在“所有路线所有画面都更好”的保证。
