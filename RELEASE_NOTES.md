# 离线视频工具箱 1.0.2 / Lite V4 更新

下载 `XeSS-Offline-Windows-x64-v1.0.2.zip`，完整解压到新目录，双击 `XeSS-Offline.exe`。不要混装旧版，既有视频与用户环境不变；不需要安装 Python、ComfyUI 或 OBS。GitHub 自动生成的 Source code 不是便携程序。

- GPU Block 默认使用 Lite V4：半宽半高双向搜索 + 全分辨率 refine。
- 修复置信度共享内存越界，并接入严格等价搜索优化。与合法 halo10 参考逐位一致，不与旧越界版本作逐位承诺。
- 三种模式、五条算法、Arc A 兼容入口、AI 深度、特效、软件/GPU/无损编码器与音轨保留继续提供；GPU DIS、AMD 光流仍是实验项。
- B580 独立光流：1080×1920 输入 3.478→2.630 ms/帧对；2160×3840 输入 13.770→10.475 ms/帧对。约减少 24%，不能当成整片加速幅度。

完整赛车 SR、人像 SR→FG 样片及候选真实后端短测通过。最终包有逐文件清单和 SHA256；新版本没有复跑历史长稳或跨显卡矩阵。范围详见 `VALIDATION.md`。
